describe('Server and database unit testing', function() {
  beforeEach(function() {
    // Do something before each test
  });

  it('does something', function() {
    expect(something).toEqual(something else);
  });

  it('does something else', function() {
    expect(something).toEqual(something else);
  });
});
